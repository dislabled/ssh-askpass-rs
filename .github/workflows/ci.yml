name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v7
      - name: Build
        run: cargo build
      - name: Test
        run: cargo test
